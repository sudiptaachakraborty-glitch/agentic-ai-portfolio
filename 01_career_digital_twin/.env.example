# Required ---------------------------------------------------------
OPENAI_API_KEY=sk-...                  # https://platform.openai.com/api-keys

# Optional (push notifications when agent gets an unknown question
# or captures a lead email). Free at https://pushover.net.
PUSHOVER_USER=
PUSHOVER_TOKEN=
